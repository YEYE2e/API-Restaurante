[project]
name = "main"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.139.0",
    "supabase>=2.13.0",
    "python-dotenv>=1.0.1"
]
[tool.fastapi]
entrypoint = "main:app"
[tool.uv]
package = false